# Task
你要完成的任务说明保存在 task.txt 文件，请完成任务。

# Instruct
0. 维持两个文件，把计划小步骤输出到 @plan.txt 文件中，执行状态保存到 @progress.txt，如果文件不存在就创建。
1. 注意制定plan.txt的时候，步骤需要详细一点。
2. 接下来，选择这次要处理的小步骤。这应该是你认为优先级最高的，不一定是列表中的第一个。
3. 检查任何反馈循环，如类型检查和测试。
4. 注意每次只执行一个步骤即可，执行完就可以退出。
5. 将你的进度追加到 progress.txt 文件。
6. 提交该步骤的 git commit，所有涉及的中间文件，都保存到 git ，方便回溯查询，一个步骤一次提交。
7. 如果在完成步骤时，你注意到所有工作任务都已完成，创建一个空文件 done.txt 。

